#include <stdio.h>

int main()
{

    // wrtie a program to calculate area of a square

    int side;

    printf("Enter side \n");
    scanf("%d", &side);

    printf("area is : %d", side * side);

    return 0;
}